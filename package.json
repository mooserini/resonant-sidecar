{
  "name": "resonant-sidecar",
  "version": "0.1.0",
  "private": true,
  "description": "Local Chrome Dev side panel for one durable agent conversation over native messaging.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mooserini/resonant-sidecar.git"
  },
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node --check native-host/*.js && node --check extension/*.js && node --check scripts/*.js && node --test test/*.test.js",
    "smoke:real": "node test/real-app-server-smoke.js",
    "check:chrome-review": "node --test test/chrome-semantic-review.integration.test.js test/review-runtime-races.test.js",
    "verify:receipts": "node scripts/verify-receipt-chain.js"
  },
  "engines": {
    "node": ">=22"
  }
}
